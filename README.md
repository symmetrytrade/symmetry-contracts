# Installation
```bash
yarn
```
# Compile
```bash
yarn build
```
# Preparation
Set `DEPLOYER_KEY` in `.env` file.
# Deploy
```bash
yarn deploy <network>
```
# Export
```bash
yarn hardhat --network <network> export --export <export_file_path>
```

